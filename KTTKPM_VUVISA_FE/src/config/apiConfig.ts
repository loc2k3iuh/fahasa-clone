const API_SERVER_PORT = 9090;
const API_SERVER_HOST = 'localhost';
const API_SERVER_PROTOCOL = 'http';
const API_SERVLET_CONTEXT = '/vuvisa';
const API_PREFIX = `api/v1`;
const API_BASE_URL = `${API_SERVER_PROTOCOL}://${API_SERVER_HOST}:${API_SERVER_PORT}${API_SERVLET_CONTEXT}/${API_PREFIX}`;

// const API_BASE_URL = '/api';

export default API_BASE_URL;
